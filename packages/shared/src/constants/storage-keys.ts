/**
 * Defines constants for storage keys used across the application.
 */

/**
 * The key used to store the primary resume data in localStorage.
 */
export const RESUME_STORAGE_KEY = 'cv-generator-resume';

// Add other storage keys as needed (e.g., user preferences, multiple profiles) 