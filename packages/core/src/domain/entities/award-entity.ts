import type { AwardInterface } from '@cv-generator/shared';

/**
 * Represents the Award entity in the domain layer.
 */
export type AwardEntityInterface = AwardInterface;

// Potential future extension:
// export class AwardEntity implements AwardInterface {
//   // ... domain logic ...
// } 